# 怀化学院本科毕业设计 LaTeX 模板 · 使用说明

> 模板名称：怀化学院本科毕业设计 LaTeX 模板 
> 
> 模板作者：**张乐冰 怀化学院 计算机与人工智能学院**
> 
> 版本：**V1.1.3**   --2026.9.10
> 
> 项目主页： https://github.com/jimmykim84/hhxy-thesis-template
> 
> 适用对象：从未用过 LaTeX 的同学（照着抄就能用）
> 
> 特别说明：本模板为非官方模板，仅参照本校学位论文格式制作，仅供排版参考；不代表学校官方要求，也不代表示例论文内容正确。如与学校最新官方文件冲突，以学校官方文件为准。

---

## 一、这个模板能帮你做什么

写毕业论文最头疼的是**格式**：字体、字号、行距、页边距、目录、页码……稍不注意就被打回重写。
本模板已经按《怀化学院本科毕业论文（设计、创作）撰写规范》把所有格式**预设好了**，你只需要：

1. 在 `main.tex` 里填上你的基本信息（题目、姓名、指导老师……）；
2. 在 `chapters/` 文件夹里写你的正文；
3. 按第四节的**编译顺序**（XeLaTeX → bibtex → XeLaTeX → XeLaTeX 四步）编译，就能得到格式规范的 `main.pdf`。

**你完全不需要懂 LaTeX 的底层原理**，照抄下面给出的"填空式"例子即可。

---

## 二、需要准备的环境

LaTeX 不像 Word 是"装一个软件就能写"，它是一套"排版引擎 + 编辑器"的组合。

### 1. 安装 TeX 发行版（必须）
它提供编译论文的引擎（里面有 XeLaTeX）。

| 系统 | 推荐安装 |
|------|----------|
| Windows | [TeX Live](https://www.tug.org/texlive/) 或 [MiKTeX](https://miktex.org/) |
| macOS | [MacTeX](https://www.tug.org/mactex/) |

> 安装时**一路默认下一步**即可，不用勾选任何额外选项。装完后在命令行输入
> `xelatex --version`
> 能显示版本号就说明成功了。

### 2. 安装编辑器（必须，用来写和编译）
推荐下面任意一个，界面友好、有"一键编译"按钮：

- **TeXstudio**（最简单，装好即可用，强烈推荐新手）
- **VS Code** + 插件 **LaTeX Workshop**（功能强，但需简单配置）

> ⚠️ **字体不用单独装！** 本模板会自动识别你当前用的是 Windows 还是 macOS，并调用该系统自带的字体（Windows 用黑体/宋体/仿宋/楷体，macOS 用 Songti/Heiti/STFangsong/Kaiti）。**换电脑写论文，什么都不用改。**

---

## 三、文件夹里都有什么

```
latex/
├── main.tex              ← 主文件：填论文信息、决定包含哪些章节
├── hhxythesis.cls        ← 模板格式文件（不要改它！）
├── refs.bib              ← 参考文献数据库（增删 / 修改文献只动这一个文件）
├── gbt7714-2005.bst      ← 参考文献著录样式（GB/T 7714-2005，随模板附带，勿删）
├── 毕业论文模版使用说明.md ← 本说明文档
├── chapters/             ← 你的正文都在这里（一章一个 .tex 文件）
│   ├── 00-abstract.tex   ← 中英文摘要与关键词
│   ├── 01-preface.tex    ← 前言
│   ├── 02-miniprogram.tex
│   ├── ……（中间是各章）
│   ├── 08-reference.tex  ← 参考文献
│   ├── 09-acknowledgement.tex ← 致谢
│   └── 10-appendix.tex   ← 附录
└── figures/              ← 你的图片放这里（支持 .pdf / .jpg / .png）
```

> 原则：**`hhxythesis.cls` 和 `gbt7714-2005.bst` 永远别动**；你要改的只有 `main.tex` 的"论文信息"、`chapters/` 里的正文文字，以及 `refs.bib` 里的参考文献。

---

## 四、第一次编译（最关键的一步）

### 编译顺序（务必照此执行）

本模板的参考文献现在用 **BibTeX** 管理：文献数据库是项目根目录的 **`refs.bib`**，著录样式由随模板附带的 **`gbt7714-2005.bst`** 控制（符合 GB/T 7714-2005，无需额外安装宏包）。因此编译需要比"纯两遍"多一次 `bibtex`，完整顺序为 **四步**：

```
第 1 遍： xelatex main.tex
         ← 排版生成第一版 main.pdf；同时扫描全部章节，把目录(.toc)、图/表清单(.lof/.lot)、所有 \label 标签和 \cite 引用键写进辅助文件(.aux 等)。
         ← 此时参考文献还显示成 [?]、目录也是空的——因为 bibtex 还没跑，LaTeX 还不知道文献长什么样。
第 2 步： bibtex  main
         ← 读取 main.aux 里实际用到的引用键，去 refs.bib 中查找对应条目，按随附的 gbt7714-2005.bst（GB/T 7714-2005 规则）格式化，写出 main.bbl（真正的参考文献列表），并把每条文献的 [1][2]… 顺序编号定下来。
第 3 遍： xelatex main.tex
         ← 读入 main.bbl，把正文里的 [?] 替换成真实编号 [1][2]…，并把参考文献列表排进文末。但参考文献一插入，页码和图表位置会整体"挪动"，本遍的交叉引用(页码、"图 2.1")仍是按旧布局算的——log 里会提示 Rerun to get cross-references right。
第 4 遍： xelatex main.tex
         ← 最后一次：布局已稳定，LaTeX 用最终位置重算目录、页码、图/表/公式编号与所有 \ref 交叉引用，全部落定。跑完 .aux 与正文一致，不再有 Rerun 警告，main.pdf 即为定稿。
```

> 💡 只要**新增 / 删除 / 修改了文献（动了 `refs.bib`）**，就必须把上面四步完整跑一遍，尤其第 2 步 `bibtex` 不能省；
> 如果只改了正文文字、没动参考文献，可以只连编两遍 `xelatex`。

### 必须用 XeLaTeX（含 bibtex 一步）

| 方式 | 操作 |
|------|------|
| **TeXstudio** | 菜单"选项→配置 TeXstudio→构建"，把"默认编译器"设为 **XeLaTeX**、"默认文献工具"设为 **BibTeX**；然后点工具栏的 ▶▶（构建并查看），它会自动按 `xelatex→bibtex→xelatex→xelatex` 执行 |
| **VS Code** | 在 `settings.json` 的 `latex-workshop.latex.recipes` 里确保有含 `bibtex` 的配方（如 `xelatex ➔ bibtex ➔ xelatex ➔ xelatex`），按 `Ctrl/Cmd+Shift+P` → `LaTeX Workshop: Build` 选用；或直接在终端连跑上面四行 |
| **命令行** | 在 `latex/` 目录下执行上面"编译顺序"里的四行命令 |

编译成功后，文件夹里会生成 **`main.pdf`**，那就是你的论文。

> **为什么不能"一遍搞定"、还要分这四步？**
> LaTeX 是"先排版、再回填"的机制：它先把章节、图表、公式、引用的编号"记在草稿（.aux 等辅助文件）里"，再回填到正文；参考文献还要额外插入 `main.bbl`，会"挤动"页码，所以必须在 bibtex 之后**再跑两遍 xelatex**，让页码和交叉引用完全正确。少跑一遍，目录和"如图 2.1 所示"里的"2.1"会是空的、文献引用会显示成 [?]。上面"四步"就是这套机制的完整顺序。

### 只编主文件 `main.tex`（不要单独编译章节）

论文**永远只编译 `main.tex` 一个文件**，各章节文件不能、也不该单独编译。

- `chapters/00~10` 都是**片段文件**，靠 `main.tex` 里的 `\input{chapters/xx.tex}` 拼进正文。它们没有 `\documentclass`、没有导言区，单独拿去编译会直接报错（"找不到文档类"）。
- `main.tex` 才是**主文件（master）**：它包含文档类、模板格式、论文信息，并 `\input` 全部章节。你每次连编两遍 `xelatex main.tex`，LaTeX 会**自动把所有章节一起读进来**排版，目录、图/表/公式的跨章编号也都在这一次整体编译里算出来。

**正确习惯**：在哪个章节里改文字都行，改完**回到 `main.tex`** 连编两遍即可。

> ⚠️ **编辑器坑**：很多人正在编辑 `chapters/02-miniprogram.tex` 时直接点"编译"按钮，结果编的是那个章节、当场报错。必须告诉编辑器"主文件是谁"：
> - **TeXstudio**：在 `main.tex` 标签页右键 → **Set as Master File（设为大师文件）**。之后无论在哪个章节，点绿色箭头都只编 `main.tex`。
> - **VS Code（LaTeX Workshop）**：在 `main.tex` 第一行加一行魔术注释 `% !TEX root = main.tex`（或确保每个章节文件顶部有这行），插件就会始终从 `main.tex` 编译。

---

## 五、配置主文件 main.tex（论文信息 / 章节挂接）

打开 `main.tex`，找到"一、论文信息"那一块，按提示把括号里的内容换成你自己的。下面是一份对照示例：

```latex
\ClassificationNumber{0809}                    % 学科分类号（问指导老师）
\ThesisType{本科生毕业设计}                     % 毕业论文 / 毕业设计 / 毕业创作
\WorkType{毕业设计}                             % 诚信声明里的称呼，一般同上一行
\TitleCN{基于微信小程序的 PMP 刷题系统的实现}       % 中文题目
\TitleEN{Implementation of the PMP Answer System Based on the WeChat Mini Program}  % 英文题目
\Author{冉\quad 娜}                            % 姓名（\quad 表示中间空一格）
\StudentID{1406404054}                         % 学号
\College{计算机科学与工程学院}                  % 学院
\Major{网络工程}                               % 专业
\Advisor{杨夷梅}{副教授}                        % 指导老师：姓名 + 职称（两个大括号）
%% 如果指导老师没有讲师及以上职称，需要第二指导老师，去掉下面这行开头的 %：
% \SecondAdvisor{某某}{讲师}
\Duration{2019.06 --- 2020.05}                % 起止日期
\SubmitDate{2020 年 5 月 10 日}                % 提交日期
```

> 注意：`\Advisor` 要写**两个**花括号，第一个是姓名、第二个是职称，缺一不可。

### 新增 / 删除一个正文章节（要动 `main.tex`）

正文每一章都是一个独立的 `.tex` 文件，由 `main.tex` 里的 `\input{chapters/xx}` 一行"挂"进论文。
**正文的章节顺序、有没有某一章，完全由 `main.tex` 里这些 `\input` 行的顺序和有无决定**；
文件名前面的 `01-`、`02-` 只是方便你在文件夹里排序，**不参与编号**——真正的"第几章"来自文件里写的 `\section{标题}`。

#### 新增一章
1. 在 `chapters/` 里新建一个 `.tex` 文件，例如 `chapters/035-relatedtech.tex`；
2. 文件第一行写章节标题（用 `\section`，和已有章节一致）：
   ```latex
   \section{相关技术概述}
   这里是这一章的正文……
   ```
3. 打开 `main.tex`，在"二、正文"那一段（`\begin{document}` 之后）找到 `\input{chapters/...}` 列表，在想插入的位置加一行 `\input`。例如插在"需求"与"技术"之间：
   ```latex
   \input{chapters/03-requirement}     % 3 需求
   \input{chapters/035-relatedtech}   % 新增：相关技术
   \input{chapters/04-technology}      % 4 技术
   ```
4. 重新编译两遍（动了章节结构后建议完整连编两遍），新章节会以正确编号出现，后面的章节编号自动顺延。

> 提示：文件名前缀随便起（`035-` 只是让它排在 `03-` 与 `04-` 之间方便查看），**关键是 `\input` 那一行写在 `main.tex` 的什么位置**。

#### 删除一章
- 临时不想要某章：在 `main.tex` 里把对应的 `\input{chapters/xx}` 行**前面加 `%` 注释掉**即可，文件先留着，以后还能恢复；
- 彻底删除：注释掉（或删掉）那行 `\input`，并可以把 `chapters/` 里对应的 `.tex` 文件也删掉。
  ```latex
  % \input{chapters/07-summary}   % 这一章暂时不放入论文
  ```

#### 注意
- `chapters/00-abstract.tex`（摘要）、`08-reference.tex`（参考文献）、`09-acknowledgement.tex`（致谢）是固定结构件，一般**不要从 `main.tex` 里删掉或调换顺序**；`10-appendix.tex`（附录）属于**可选内容**，论文没有附录时可以把对应的 `\input` 行删掉。你能自由增删的，是 `01-preface` 到 `07-summary` 之间的"正文主体章节"。
- 章节里的 `\section` 编号由模板自动连续生成：增删或移动某一章后，只要重新编译两遍，后面所有章节的"第 N 章"会自动重新编号，不用手动改数字。

---

## 六、写正文（改 `chapters/` 里的文件）

正文直接写中文就行，**首行缩进、行距（固定 22 磅）、仿宋字体都是模板自动处理的**，你不用管。

### 1. 章节标题
```latex
\section{微信小程序开发介绍}          % 一级标题（会显示为"2 微信小程序开发介绍"）
\subsection{系统架构}                  % 二级标题
\subsubsection{典型界面布局}          % 三级标题（更细的划分）
```
写完后保存，重新编译两遍，目录和编号会自动更新。

### 2. 普通段落与强调
```latex
这是一段普通文字，直接写即可，回车换行在 PDF 里不会分段。

空一行才会另起一段。需要加粗用 \textbf{这几个字会加粗}。
```
行内出现代码、命令、文件名时，用等宽字体：
```latex
调用 \texttt{wx.request} 接口发起网络请求。
```

### 3. 列表（条目式内容）
```latex
\begin{enumerate}      % 自动编号 （1）（2）（3）
  \item 用户可以选择答题数量；
  \item 用户可以进行在线答题；
  \item 答题后可查看解析。
\end{enumerate}

\begin{itemize}         % 无编号的圆点列表
  \item 第一条要点；
  \item 第二条要点。
\end{itemize}
```

---

## 七、插图、表格、公式（重点）

这三项是毕业论文里最容易出格式问题的地方，模板已经帮你定好了**编号规则（按章编号，如"图 2.1""表 3.2""式 (4.1)"）**和题注位置（图题在下、表题在上）。下面给的都是可直接套用的模板。

### 1. 插图
把图片文件（建议导出为 **PDF** 或 **JPG/PNG**，矢量图 PDF 最清晰）放进 `figures/` 文件夹，然后：

```latex
\begin{figure}[htbp]
  \centering                                   % 图片居中
  \figurenote{图中箭头表示数据与事件的传递方向。}   % 可选：图注（写在图题上方）
  \includegraphics[width=0.8\textwidth]{figures/fig-architecture.pdf}  % 宽度设为页面宽度的 80%
  \caption{刷题系统结构层次图}                    % 图题（自动编号，在下）
  \label{fig:architecture}                      % 标签，用于正文引用（名字自取，建议 fig:xxx）
\end{figure}
```

正文里引用这张图（引用时**不要**手写编号，让模板自动填）：
```latex
微信小程序的结构层次如图 \ref{fig:architecture} 所示。
```
> - `width=0.8\textwidth` 表示图片宽度为**正文的 80%**，可按需要改成 `0.5`、`1.0` 等。
> - 图片文件名要和 `\includegraphics` 里写的一致（含扩展名）。
> - **先写 `\label`，再编译两遍**，`\ref` 才会显示正确编号。

### 2. 表格
```latex
\begin{table}[htbp]
  \centering
  \caption{授权登录测试用例}           % 表题在上（自动编号）
  \label{tab:test-login}              % 标签，建议 tab:xxx
  \begin{tabular}{|c|p{9.6cm}|}        % 两列：第一列居中(c)，第二列宽 9.6cm 自动换行
    \hline                           % 横线
    用例名称 & 授权登录 \\
    \hline
    目\quad 的 & 测试获取用户信息授权的功能 \\
    \hline
    测试流程 & 用户第一次进入小程序，弹出系统授权框，点击"允许" \\
    \hline
    预期结果 & 成功获取用户头像、昵称 \\
    \hline
  \end{tabular}
\end{table}
```
引用方式：`授权登录的测试用例如表 \ref{tab:test-login} 所示。`

> - 列格式 `|c|p{9.6cm}|` 中，`c`=居中、`l`=左对齐、`r`=右对齐、`p{宽度}`=固定宽度可换行。几列就写几个，用 `|` 分隔即加竖线。
> - `\hline` 画一条横线；`&` 分隔同一行的列；`\\` 换行。
> - 表里某格没数据，写 `\voiddata`（显示规范的"—"）。

### 3. 公式
**行内公式**（夹在文字里）用一对美元符号 `$...$`：
```latex
手机主屏分辨率为 $1920 \times 1080$ 像素。
```

**独立成行的公式**（自动按章编号、居中）用 `equation` 环境：
```latex
本次答题的正确率按式（\ref{equ:accuracy}）计算：

\begin{equation}
  P = \frac{N_{\mathrm{right}}}{N_{\mathrm{total}}} \times 100\%
  \label{equ:accuracy}
\end{equation}

式中：$P$ 为正确率；$N_{\mathrm{right}}$ 为答对题数；$N_{\mathrm{total}}$ 为总题数。
```
引用方式：`按式（\ref{equ:accuracy}）计算`。

**多行对齐的公式**（如推导过程），用 `align` 环境（模板已内置支持）：
```latex
\begin{align}
  a &= b + c \\
    &= d + e + f
\end{align}
```
> 公式中常用符号速记：`\frac{分子}{分母}` 分数、`\times` ×、`\leq` ≤、`\geq` ≥、
> `\sqrt{x}` √x、`x^{2}` 上标、`x_{1}` 下标、`\sum` ∑、`\int` ∫。

---

## 八、摘要、致谢、参考文献、附录

这些部分已经在 `chapters/` 里预置好了，你**只改文字，不要删环境命令**。

### 1. 中英文摘要（改 `00-abstract.tex`）
```latex
\begin{cnabstract}
这里是中文摘要正文……
\end{cnabstract}

\begin{cnkeywords}
微信小程序；PMP；移动端答题      % 关键词用中文分号隔开
\end{cnkeywords}

\begin{enabstract}
This is the English abstract……
\end{enabstract}

\begin{enkeywords}
WeChat Mini Program; PMP; Answer in mobile terminal
\end{enkeywords}
```

### 2. 致谢（改 `09-acknowledgement.tex`）
```latex
\begin{acknowledgement}
感谢我的指导老师……（此处写致谢正文）
\end{acknowledgement}
```

### 3. 参考文献（数据库在 `refs.bib`）
所有文献都写在项目根目录的 **`refs.bib`**（标准 BibTeX 数据库）里，增删条目、调整内容只改这一个文件；`chapters/08-reference.tex` 仅两行（`\bibliographystyle{gbt7714-2005}` + `\bibliography{refs}`），一般不用动。

每条文献是一个 `@类型{引用键, 字段=...}` 块，正文里用 `\cite{引用键}` 引用（**自动变成上标 [1]**，编号由 `bibtex` 按 GB/T 7714-2005 顺序编码制自动生成）：

```latex
%% 这是 refs.bib 里的内容（每条一个 @条目）

@book{bib:lei,                       % 引用键自取，建议 bib:xxx
  author    = {雷磊},
  title     = {微信小程序开发入门与实践},
  address   = {北京},
  publisher = {清华大学出版社},
  year      = {2017},
  pages     = {121-153}
}

@article{bib:wang2021,
  author  = {Wang, Y. and Chen, L. and Liu, H.},   % 西文作者：姓, 名缩写
  title   = {Design and Implementation of a Mobile Learning System Based on WeChat Mini Program},
  journal = {International Journal of Emerging Technologies in Learning},
  year    = {2021},
  volume  = {16},
  number  = {5},
  pages   = {128-142}
}

@online{bib:wechatdoc,               % 电子文献用 @online
  author     = {腾讯科技},
  title      = {微信小程序开发文档},
  year       = {2023},
  modifydate = {2023-05-10},
  citedate   = {2024-03-20},
  url        = {https://developers.weixin.qq.com/miniprogram/dev/framework/}
}
```
正文引用：`已有大量研究\cite{bib:lei,bib:wang2021}表明……`（多个文献用逗号隔开）。

> 文献格式请严格按学校规范（GB/T 7714-2005 顺序编码制）著录，模板只负责排版样式。
> 著录细节（作者、题名、刊名、年卷期页码、电子文献的更新/引用日期与网址）由 `gbt7714-2005.bst` 控制，你只需在 `refs.bib` 里填对字段即可。
> **西文作者姓名默认混排大小写**（如 `Zakas N C`、`Wang Y, Chen L`）；若想恢复成全大写（如 `ZAKAS N C`），把 `gbt7714-2005.bst` 中 `format.names` 函数里对姓氏的 `"t" change.case$` 改回 `"u"` 即可（中文姓名不受影响）。
> **注意**：改完 `refs.bib` 后，必须重新跑一遍"编译顺序"里的四步（含 `bibtex`），否则参考文献不会更新。

### 4. 附录（改 `10-appendix.tex`）
附录里放核心代码、补充推导等。注意：**附录中的公式/图/表编号会自动变成 A1、A2……**

```latex
\begin{theappendix}

\subsection*{A1\quad 首页视图层结构}     % 带星号的 subsection 不编号

\begin{lstlisting}[style=js, caption={洗牌动画 onShuffleEvent}]
/* 这里直接贴你的代码 */
function foo() { return 1; }
\end{lstlisting}

\begin{equation}
  C = \left\lfloor \frac{Q}{20} \right\rfloor - U
  \label{equ:appendix-count}
\end{equation}

\end{theappendix}
```
> 代码环境支持两种风格：`style=js`（JavaScript）、`style=xml`（HTML/XML 类标记）。

---

## 九、常见问题与排错

**Q1：编译报"找不到字体 / Font XXX not found"？**
- 本模板已自动识别系统。若你**手动**在 `\documentclass` 后加了 `[winfonts]` 却在 Mac 上编译（或反之），就会找不到对应字体。
- 解决：把 `main.tex` 第一行的 `\documentclass` 恢复成 `\documentclass{hhxythesis}`（不写任何字体选项），让模板自己判断。

**Q2：目录空白 / 正文里"如图 X 所示"的编号是空白或问号？**
- 没编译够两遍。重新用 XeLaTeX **连编两遍**即可。
- 若正文里的文献引用显示成 **`[?]`**、或文末参考文献列表空白，说明漏了 `bibtex` 这一步——请按第四节完整跑四步（含 `bibtex main`）。

**Q3：图片显示不出来？**
- 检查三点：①图片是否真的在 `figures/` 文件夹；②`\includegraphics` 里的文件名（含扩展名）是否完全一致；③格式是否为 PDF/JPG/PNG。

**Q4：改了内容，PDF 没变化？**
- 确认你改的是 `chapters/` 下对应的文件，且改完**重新编译了两遍**。

**Q5：编译时弹出一堆辅助文件（.aux/.log/.toc 等）？**
- 这些是正文编译过程产生的"草稿"，**可以放心删除**，不影响论文；下次编译会重新生成。保留 `main.pdf` 和所有 `.tex` 源文件即可。

**Q6：中文变成乱码或问号？**
- 确保你的 `.tex` 文件是 **UTF-8 编码（无 BOM）** 保存的。用 TeXstudio / VS Code 默认就是，不要手动存成 ANSI/GBK。

---

## 十、一句话流程（贴墙备忘）

> **填 `main.tex` 信息 → 写 `chapters/` 正文 → 按第四节"四步"编译（XeLaTeX→bibtex→XeLaTeX→XeLaTeX）→ 看 `main.pdf`**
> 只改了正文、没动参考文献时，可只连编两遍 XeLaTeX；动过 `refs.bib` 必须把四步完整跑一遍。出问题先看上表"常见问题"，九成能自己解决。

祝各位同学顺利毕业 🎓
